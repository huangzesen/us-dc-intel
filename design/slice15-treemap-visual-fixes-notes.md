Reveal washout/blank capture: made the treemap card render at full opacity instead of depending on the scroll reveal observer.
Oversoft light-theme tiles: strengthened the SVG and legend gradients so swatches continue to match tile fills while remaining saturated in light mode.
Tiny centered labels on large tiles: moved value-bearing labels to padded top-left positions and increased the responsive label size cap.
Dense bottom-row striping: reduced treemap gutters and scaled tiny-tile corner radii/strokes so micro tiles read as clean rows instead of black zebra bands.
Native-looking drill back button: added explicit treemap back-button background, typography, hover color, and cursor styling.
